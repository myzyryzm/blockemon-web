/** @format */
import fs from 'fs'
import nearAPI, { Account } from 'near-api-js'
import getConfig from './config'
const { nodeUrl, networkId, contractName, contractMethods } = getConfig()

export {}
// todo: create contract
